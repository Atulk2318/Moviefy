# Rename this file to secrets.toml (locally) or paste its contents into
# the "Secrets" section of your app on Streamlit Community Cloud.
# Never commit the real secrets.toml file to version control.

OMDB_API_KEY = "81038dd6"
